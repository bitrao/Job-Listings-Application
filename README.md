# Job Listings Application

## Run the project

```python
# Init pipenv (do this first)
pip install pipenv
pipenv install

# Start the project
pipenv run start

# Install new package
pipenv install 
```
